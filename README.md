# yield-aggregator-strategy-base
An expert-level Yield Aggregator strategy template for auto-compounding rewards. Built to interface with lending protocols (like Aave) or AMMs (like Uniswap) to maximize LP returns through automated harvesting.
